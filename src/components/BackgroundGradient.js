import React from 'react';

const BackgroundGradient = () => {
  return (
    <div className="absolute inset-0 w-full h-full rounded-full bg-gradient-to-r from-[#58AEF1] to-pink-500 -top-12 -right-14 blur-2xl opacity-10 z-0"></div>
  );
};

export default BackgroundGradient;
